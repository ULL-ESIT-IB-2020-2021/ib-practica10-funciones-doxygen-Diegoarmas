#include <iostream>
#include <cstdlib>
#include "funciones_cripto.cc"


int cript_cesar(const size_t codificador, const size_t clave){
    
}






int main(int argc, char* argv[]){
    Usage(argc, argv);
    std::string limit = argv[3];
    const size_t codificador = stoi(limit);
    std::string limit2 = argv[4];
    const size_t clave = stoi(limit2);
    
    return 0;
}

